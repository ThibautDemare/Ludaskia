/* ============================================================
   Synthèse vocale (dictée) — détection best-effort.
   Stub : la dictée TTS sera implémentée dans une issue dédiée
   (voir docs/design-orthographe.md, § Dictée / issue accessibilité).
   Pour l'instant la dictée n'est pas proposée (mode best-effort off).
   ============================================================ */
export function dicteeDisponible(): boolean {
  return false;
}
